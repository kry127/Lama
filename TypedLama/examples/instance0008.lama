-- callable checking
succ :: Int ~> Int;
fun succ(x) { x + 1 }

succSmoker :: ? ~> Int;
fun succSmoker(x) { x + 1 }

succAccepter :: Union[Int ~> Int, ? ~> Int];
fun succAccepter(x) { x }

local x = true;
succAccepter(if x then succ else succSmoker fi); -- should type check
Unit