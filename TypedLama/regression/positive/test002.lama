-- functions are not supporting currying
public fun kCombinatorTyped (x :: Int, y :: Str) :: Int {
  x
}

-- but how we would still type pattern-matching?
-- Answer: we should infer type!
public fun id2 (Abc (x, y)) :: ? {
  x
}


write(kCombinatorTyped(4, "y"));
write(id2(Abc(3, 5)));
Unit
