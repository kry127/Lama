-- допустим есть несколько объектов неизвестного типа ?
local a :: ? = A(3, "three"); -- S-выражение A от числа и строки
local b :: ? = fun(t) { 4 };  -- замыкание, возвращающее число и игнорирующее входной аргумент
local c :: ? = 35;            -- число


{
  -- пусть мы хотим в рантайме проверять использование объектом типа подобного вида
  obj :: Union[A(Int, Str); B(C, Int) -> Str];
  local obj;

  -- оба присвоения должны проверять в рантайме, что мы присваиваем объект правильного типа
  obj := a;
  printf("b");
  obj := b; -- TODO ok, but think about real execution of obj!
  obj := c; -- Runtime error, "C’est la vie"
  Unit
};
Unit